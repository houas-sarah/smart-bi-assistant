# Streamlit secrets template.
# For local runs: copy this file to `.streamlit/secrets.toml` (git-ignored),
# OR just use a .env file — either works.
# For Streamlit Community Cloud: paste these lines into
# Advanced settings -> Secrets when deploying.

HF_API_KEY = "hf_your_token_here"
HF_MODEL_NAME = "Qwen/Qwen2.5-Coder-32B-Instruct"
